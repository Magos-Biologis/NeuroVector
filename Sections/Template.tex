% This exists as a template for copy pasting (It lets you generate the individual file with the main preamble)
% Copy paste the shit below into a new file 
% And write everything between the "begin" and "end" document
% That's the best way to experiment
% \documentclass[../../Orator]{subfiles}
\documentclass[class={myRUCProject}, crop=false]{standalone}
\IfStandalone{%
    \import{}{customCommands}
    \import{}{INP-00-glossary}
    }{}
    
\begin{document}


\end{document}